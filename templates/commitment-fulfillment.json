{
  "id": "commitment-fulfillment",
  "version": "1.0.0",
  "title": "Commitment fulfillment",
  "short": "commitment ledger",
  "priority": 60,
  "spec": "research/13-sybill-deep/05-template-library.md section 4, template 4",
  "situation": "An earlier call on this deal left the rep holding a promise, and this call is where it gets reconciled.",
  "notes": "The one template genre the Sybill sweep found no equivalent for. The ledger slot reads promises from EARLIER calls on the same deal, so its claim ids are namespaced by call id. They are gate-verified claims like any other, and the screen checks them the same way.",
  "panel": {
    "explainer": "An open promise from an earlier call picked this template. A free model drafted it. The gate checked every line."
  },
  "subject": "Where things stand",
  "word_limit": 120,
  "routing": {
    "trigger": {
      "all_of": [
        { "scope": "deal", "metric": "open_rep_promises", "min": 1 },
        { "section": "next_steps", "extractor": "next_steps", "where": { "owner": ["rep"] } }
      ]
    }
  },
  "blocks": [
    { "type": "text", "role": "greeting", "text": "Hi {{recipient}},", "fallback": "Hi there," },
    { "type": "text", "role": "opener", "text": "Thanks for the time today. Quick check on where things stand." },
    { "type": "slot", "role": "recap", "label": "What I promised", "scope": "deal", "source": "open_rep_promises", "limit": 3, "hint": "Promises the rep made on earlier calls. Each one carries the call it was made on." },
    { "type": "slot", "role": "recap", "label": "What we covered", "section": "objections", "limit": 2, "hint": "What the buyer said about those promises on this call." },
    { "type": "slot", "role": "next_steps", "label": "New from today", "section": "next_steps", "extractor": "next_steps", "hint": "What was agreed on this call, with owner and due date." },
    { "type": "instruction", "text": "Rewrite the lines above as one short email in the second person. Every sentence that says something about a call must carry the claim id it came from. Name a promise as delivered only when a claim says so. Never mark something done to make the ledger look better. Short sentences." },
    { "type": "text", "role": "assurance", "text": "Every line above came from something said on a call. If I got any of it wrong, tell me and I will fix it." },
    { "type": "text", "role": "signoff", "text": "Best,\n{{sender}}", "fallback": "Best," }
  ]
}
