# SYNC — cross-session coordination log

> **Canon precedence:** `DECISION-BRIEF.md` is the spec. L1–L19 are evidence-locked; changes route through the standing auditor (via the hackathon session). This file logs coordination and decisions only — it never forks the spec. Then: `research/00-api-probe/FINDINGS.md` (+ addendum) → `research/01–05` → `audit/`.

**Sessions:**
- `hackathon` — spec/integration owner (proposed, pending D5), auditor relay, API probes + fixtures lineage
- `projects-2f` — fresh build session (Sourav, Aug 13 ~13:00): Phase 0 skeleton + Phase 1 ingest, claim on the board to start
- ~~`marketing`~~ — REMOVED from this project per Sourav (Aug 13 ~13:05): today's only focus is the hackathon; the JustCall-marketing session does not participate or edit this repo

**Conventions:** log every decision below with the L-number(s) it touches. Format: `date · who · decision · L-refs`.

---

## Decision log

- 2026-08-14 ~03:00 · Sourav (via projects-2f) + execution · **MERGE EXECUTED, both directions.** Sourav's ruling: best of both builds, updated and pushed. Her-side motion: gate-hardening drafted against Saritha's repo, verified (her suite 32/32 incl. new regression file; 21/21 adversarial vectors), delivered as DRAFT PR #1 from Sourav's account (sarithakonudula/open-gong-lite#1) — she controls the merge. Patch closes 5 paths: empty/short-quote verify, digit-fusion laundering, bestEvidence self-certified receipts, fabricated-L1-on-empty-transcript, invented "Follow up on:" next steps (absence honesty). Her email choke + injection ports passed EVERYTHING — credit recorded. Our-side motion: her best parts already grafted (samples-first landing, keyless fallback chain, setup story — c0bec7b made ours 2-command no-install; her copy angle credited in hero). Base-vs-base final call for Friday remains Sourav's with the synthesis; BOTH repos now carry the same fabrication regression set, so the moat survives either choice. · L-refs: L6, L7, L8; supports §1 wedge

- 2026-08-14 ~01:30 · projects-2f · Scorecard RED cleared: ld-5.4 counts footer — viewer header now shows the gate's own scorecard on every render ("✓ N verified · N corrected · ⚠ N uncorroborated · ⛔ N blocked — dropped claims stay visible, nothing is silently removed"); computed in buildViewModel (pure, tested: every claim lands in exactly one bucket), rendered in header + inherited by tier-1 exports automatically. hero.png re-captured with the footer visible. Evidence: scorecard row 🟢 ("dropped-count footer found in src/viewer.js"), weighted total 38→41; viewer/export suites 18/18; full suite 276 pass 0 fail. · L-refs: L7, L17

- 2026-08-13 ~23:45 · projects-2f · TRACKER JSON DROPPED on hackathon's green (aad40e3 pulled first): extractors/tracker.json — 8 seed keywords spanning fixture terms + arc-agnostic sales terms (content owner edits the list when DEAL-STATE lands; that edit is data, not code). Evidence: registry loads 7 families (`…, summary, tracker`); npm test 238/238 fresh. **SLICE 2 FUNCTIONALLY COMPLETE** — board banner added; both code lanes quiet at the Slice-3 content boundary; single-unlock framing confirmed (key rescope → probe + TTS; D4 → extraction/bake-off/email-polish; D3 → happy-path proof). · L-refs: L12; Stage-5 P2 tracker family

- 2026-08-13 ~23:15 · hackathon · D2 ACK: deal arc locked 1:1 (unblocks Slice-3 scripts on format). 3-voice mono diarize probe = NOT YET RUN (hour-zero probes died on sandbox 403/429 before reaching it; round-3 addendum never covered it). QUEUED against the rescoped key (needs a working PyAI key; current live key flagged for revoke/rescope, so not running the probe on a credential I've recommended killing — non-blocking per Sourav, so queue is correct). Probe spec: 3 distinct PyAI TTS voices → mix to MONO → jobs API diarize=true → does Sortformer split ≥2 speakers? Result logged against D2 when the key lands. Slice-2 tracker enablement (gate double-mark guard + speaker_mismatch enum + registry keywords/tracker role + extract.js deterministic dispatch) building now; tracker JSON held on peer side until my green ping. · L-refs: L2, L15

- 2026-08-13 ~23:00 · Sourav (via projects-2f, structured question) · **D2 RULED: both.** (1) Deal arc LOCKED as five 1:1 calls (one rep, one prospect — perfect channel diarization by construction; script-writing unblocked for Slice 3). (2) AND run the 3-voice mono probe anyway — non-blocking insurance/knowledge, does not gate the arc; if a third speaker ever splits it expands future options, nothing more. Probe routed to hackathon (probe lane owner — confirm whether hour-zero already produced this verdict before re-spending minutes). Still open: D3 real dual-channel recording · D4 Anthropic key + cap · live-key rescope. · L-refs: L15, L16; D2 closed

- 2026-08-13 ~22:00 · hackathon · Email choke point (src/email.js) AUDIT PASS-WITH-FINDINGS — merge-blessed. Deterministic demo path airtight: injection payload cannot reach any email body (38/39 vectors fail closed; assertClaimsOnly rejects transcript-shaped input, EMAILABLE Set exact-match, whole-draft-rejection on unknown/unverified claim_id). Stage injection claim HOLDS. Findings tracked as REQUIREMENTS on the future D4-gated LLM-polished path (not blocking): F-1 screenDraft must validate bullet TEXT-containment vs cited claim (id-only today); F-2 escape body if ever HTML-rendered; trust-boundary README note (composeEmail trusts gate-assigned status). Slice-2 extractor JSON files claimed by projects-2f. · L-refs: L8

- 2026-08-13 ~22:30 · projects-2f · P1 EXTRACTOR FILES SHIPPED: next_steps (owner side + verbatim due timeframe — the DiscoveryClaude gap; required_section per coverage banding), pain (buyer's words + verbatim impact; negation/hypothetical/reported-speech excluded in prompt per Stage-2 sub-params 2.2), pricing (numbers copied EXACTLY as rendered — digit conversion warned as verification-fatal), competitors (active_user vs migrating_from distinction). All with crm_map blocks (ai_* property names per synthesis ruling), evidence-before-text key order, portability-lint-clean schemas. Evidence: all 6 families load through the frozen registry validator (`loaded: competitors, next_steps, objections, pain, pricing, summary`); npm test 215/215. KEYWORD TRACKER blocked, routed to hackathon: frozen registry rejects a `keywords` field (ALLOWED_KEYS) and extract.js has no deterministic dispatch — proposed design: role "tracker" + top-level keywords + capabilities.roles.tracker {provider: "deterministic"}; JSON file is 10 min once those two land. · L-refs: L12; Stage-5 P1 families

- 2026-08-13 ~22:00 · projects-2f · Slice-2 rows delivered: (1) EMAIL CHOKE POINT (L8) — src/email.js: composer structurally refuses bundle/transcript-shaped input (the transcript cannot reach the email role even by accident); only verified/segment_corrected claims can enter a draft; screenDraft() cuts uncited bullets and rejects the WHOLE draft on any unknown or non-verified claim_id (spec-core whole-answer-rejection); deterministic keyless baseline included, LLM-polished path (D4) will pass the same screen. 6 tests incl. injected-text-never-leaks. (2) README "Known limitations" block — follow-ups (b)+(d) closed: hyphen/slash false-demotion stated (prefer false demotion over loosened matcher), taint-screen best-effort + its containment layers, cross-utterance cue gap, "right quote wrong claim" named unsolved. Evidence: npm test 215/215 fresh. · L-refs: L7, L8, L17

- 2026-08-13 ~21:30 · hackathon · **SLICE 1 ALIVE — gate PASS (3rd audit), integration smoke green, pushed to remote (af45d99).** Gate fabrication-safe: 2 FAB paths from the F-2 punctuation fix closed (whitelist strip + digit-flank guard + whitespace-only reject), re-audited PASS, no plausible quote (word or valid number) verifies where it doesn't occur. End-to-end smoke: fixture call → gate → extraction runner → bundle → server → viewer; audio Range 206 (click→timestamp→play chain works); tier-1 HTML export renders all 4 claim states. 209/209. SLICE-2 FOLLOW-UPS (non-blocking, logged): (a) double-mark digit fusion "3..30"→"330" — tighten guard to nearest-non-punct-neighbor-is-digit + test; (b) hyphen/slash false-rejection ("follow-up" vs unpunctuated "follow up") — README limitation + optional stage-2b strip-when-not-digit-flanked; (c) speaker_mismatch → add to evidence.json context_flag enum; (d) README limitations: taint-screen-is-best-effort + cross-utterance cue gap. · L-refs: L6, L7, L12

- 2026-08-13 ~20:15 · projects-2f (per Sourav) · AAKASH ROUND 3 FILED: architecture-comparison (OSS sweep + reconstructed Sybill architecture) → research/11 inbox verbatim + SYNTHESIS.md addendum (pending hackathon ratification per A1). Adopted: two-camp lane-empty map + upgraded pitch line; NEW kill-line (Meetily paywalls diarization out of its 29K★ OSS edition — ours is free from stereo, L2); judge-proof lines (Sybill also outsources ingestion / drafts-never-sends / batch-only — our cuts are their production architecture); confidence-amber render as Slice-2 candidate; merged positioning "Sybill's architecture, Gong's target, zero lock-in — and receipts none of them have." FLAGGED: the doc benchmarks against build-plan.md (the REJECTED PM-spec ingestion shape) — its "our (planned)" column (no ASR / HubSpot-as-DB / Next.js) must never be quoted as our architecture (conflicts L1/L2/L10 + spec-core). LOCAL COMMIT, push hold stands. · L-refs: L1, L2, L8, L10; supports §1 wedge

- 2026-08-13 ~19:50 · projects-2f (per Sourav) · SARITHA'S LANE-10 PACKAGE REVIEWED + INTEGRATED. Verified her gate_chain_verify.mjs at HEAD (post my transcript.js changes): clean Sonnet-5 run reproduces 3/3 match_exact, adversarial file fires all four L7 verdicts — her tool survives the churn and is a genuine independent cross-check lineage vs src/gate.js (two implementations, one spec: divergence = a bug somewhere). Her capabilities.json schema-gap finding CLOSED: roles.extraction gains fallback_model (null until lane-10 promotion). Answered her via 💬 comment in her FINDINGS incl. the L7-adjudication caveat on her stage-2. Increments unlocked when D4 (Anthropic key) lands: full Haiku/Opus bake-off through her harness against BOTH fixtures (stereo + the F-21 "forty/40" stress case) is one command per model. Her adversarial fixture remains earmarked as Phase-2's planted-fake-quote exit test (hackathon runner already fixtures it independently — converging evidence). LOCAL COMMIT ONLY, push hold stands. · L-refs: L6, L7, L12

- 2026-08-13 ~19:30 · projects-2f · Slice-2 tier-1 export SHIPPED (offered row) + A-007 mono residual FIXED (~3-line text-split fallback in monoUtterances, regression test added). src/export.js: one self-contained HTML file (viewer + CSS + bundle inlined; every `<` in the JSON escaped to < so a spoken "</script>" is inert — breakout test included; CSP meta injected; bundle validated through buildViewModel at EXPORT time so an invalid bundle fails before it ships). Viewer gains inline-mode boot (#og-data) — same module, both fuel lines. Real 12KB share file generated from the fixture bundle and handed to Sourav. Evidence: scoped suites 42/42 (full npm test deferred while gate remediation edits gate/injection/registry mid-flight; will re-run at their green push). COMMITTED LOCALLY ONLY per hackathon's hold — push happens with their coherent Slice-1 green. · L-refs: L11, L17, L19; A-007 residual closed

- 2026-08-13 ~18:40 · hackathon · Slice-1 viewer/server lane AUDIT PASS — merge blessed. Re-verified at HEAD 7b392ac: 4 blockers + serious findings all FIXED (process-kills survive + answer next request, XSS dead at both layers, poll floor 300s, status enum fail-closed, id-keyed anchors, signal-aware backoff, suffix-Range RFC, HTTP-date Retry-After). 139/139 green. Residual (Slice-2, non-blocking): mono no-words[] path still emits one 600-word utterance (A-007 BOTH-paths violation, ~3-line fix). Gate-core audit + extraction runner still in flight. · L-refs: L7, L13, L19
- 2026-08-13 ~18:40 · hackathon · API round-3 facts logged (research/00 addendum 12-14): quota via GET /v1/me; async params channel/diarize/numerals/output_formats confirmed; English-only transcription today; trace incompatible with diarize/channel; no nova model (L5 reconfirmed). KEY HYGIENE: browser agent minted over-scoped uncapped live key + tripped safety classifier — flagged to Sourav to revoke/rescope; build runs keyless through Slices 1-2; no key in git (verified). · L-refs: L5

- 2026-08-13 ~19:00 · projects-2f · AUDIT FIXES DELIVERED — all 9 findings + the 3 also-rans, test-first. Blockers: server content-read-before-headers + stream error listeners + headersSent-aware fail path (missing bundle → 500 and the process survives, verified by test); hostile utterance id rejected at boundary (integer-validated + escaped); poll floor restored to 300s. Serious: claim.status validated against the closed enum, unknown → THROW (fail closed); 40-word cap now holds with empty/mismatched words[] via text-split fallback w/ interpolated times; anchors id-keyed via Map (reordered bundles resolve; missing cited utterance fails at LOAD); pyaiFetch reworked to total-elapsed budget + interruptibleSleep(signal) + named PYAI_TIMEOUT/PYAI_ABORTED exits; upload timeout scales ~10s/MB; suffix Range now RFC-correct (last n bytes); classify429 parses HTTP-date + 60/61 boundary tested; estimate divisor now safe for low-bitrate audio. Evidence: `npm test` 139/139 (incl. gate stack's 125), live server 200/206/206. NOTE: 3f14226 swept my 3 mid-flight files (server/transcript/viewer) into the gate commit — content correct, tests green, no revert needed; logged as an A2 anti-pattern instance (commit swept another session's in-progress files). · L-refs: L13, L14, L17, L19

- 2026-08-13 ~18:30 · projects-2f (per Sourav) · Front-door refresh: README + START-HERE state sections updated from "build GATED/frozen" (stale since the ~17:20 go) to "build IN FLIGHT, Slice 1" with what's landed vs in flight; src/ map row updated (viewer + server added, frozen-wording removed); GitHub repo description set. Committed narrowly (3 files only — hackathon's builders have WIP in this tree, no `git add -A`). · L-refs: none (process)

- 2026-08-13 ~18:15 · projects-2f · Cached demo path shipped (offered row, my files): `npm run demo` serves the committed fixture bundle + committed call.wav — zero keys, zero network (L17). Verified live: viewer 200, 4 claims served, audio Range 206; `npm test` still 22/22. When gate.js emits the real sample-deal bundles, the demo command gains argv paths — no code change. Awaiting auditor verdict on the Slice-1 diff. · L-refs: L17

- 2026-08-13 ~18:00 · projects-2f · SLICE-1 LANE DELIVERED (test-first throughout). (1) Defect fixes: stereo segments now split at the 40-word cap + utterances time-sorted w/ sequential ids (A-007); every fetch carries AbortSignal.any(caller, 30s timeout); pollJob timeout scales w/ estimated audio duration (byte estimate errs long = safe); 429 discriminated on Retry-After (≤60s → jittered retry ×2, absent/long/garbage → PYAI_DAILY_CAP). (2) Minimal viewer + app server: src/viewer.js (pure view-model + render; escaping tests written before escaper), src/viewer.html, src/server.js (127.0.0.1:4317, zero deps, Range/206 verified live: `206 bytes 100-199/677600`); fixture bundle test/fixtures/bundle.slice1.json exercises all 4 claim.status states; view-model THROWS if a notes block cites a blocked_injection claim. Evidence: `npm test` → 22/22 fresh. (3) phase-3-ui.md reconciled: Q1/Q2/Q3 resolved per technical-spec-core rulings, blocked state added to the machine, representation.md noted as governing Slice-2 section anatomy. Ready to integrate the real gate output: viewer consumes any bundle matching spec §2 shapes — swap /bundle.json when gate.js lands. · L-refs: L6, L7, L11, L13, L17, L19

- 2026-08-13 ~17:20 · Sourav (via hackathon) · **BUILD GATE OPEN — "go."** Slice 1 starts now per team/plans/build-orchestration.md. Claims: hackathon → gate/extraction stack (gate.js, injection.js, registry, prompt, llm, extract, run records) via constructed-context subagents; projects-2f → 4 defect fixes in its kept code (stereo max-length split + time-sort in transcript.js, AbortSignals in pyai.js, 429 Retry-After discrimination) + minimal Slice-1 viewer (ONE click-claim→line→audio interaction, WAV+preload). Bake-off does NOT block: default claude-sonnet-5; Saritha's result = one config line later (L12). Hour-zero probes running (long-audio, dual-mono, numerals, 3-voice). Slice-1 exit: planted fake quote visibly demoted on screen + npm test green offline → evidence report to Sourav. · L-refs: L6, L7, L12, L14

- 2026-08-13 ~17:15 · projects-2f (per Sourav) · READER-FACING CLEANUP (coordinated with hackathon session; files cleared, no overlap): README.md rewritten as the true front door (end goal incl. Show HN prize, one-line state, 3-rule collab model, defers to START-HERE for onboarding); TASKBOARD.md rewritten to current-truth-only (done-foundation table w/ evidence, gated build slices from master plan, active research lanes w/ R-10 marked CRITICAL PATH, pruned open lanes, correction blockquotes removed — history stays here in SYNC); phase-1-ingest.md decision block now records Sourav's ~15:05 "keep as-is, no audit" APPROVAL + commit 36e8ce2 + fresh 11/11 test evidence (INDEX.md row updated to resolved); ONBOARDING reading order aligned to START-HERE; CLAUDE.md bootstrap now includes master-plan. Fresh verification this entry rests on: `npm test` → 11/11. · L-refs: none (process)

- 2026-08-13 evening · cleanup/coherence agent · **CORRECTION, not a decision:** the
  ~14:10 entry below states Phase-1 (`src/{ingest,transcript}.js` + tests) was "left
  UNCOMMITTED per freeze order." That is stale — `git log` shows all four files were
  swept into commit `36e8ce2` ("Fold Aakash findings...; commit master plan") when the
  master plan's "PUSH EVERYTHING NOW" step ran. The code is now in the repo on `main`.
  The formal APPROVE/REJECT checkbox in `team/plans/phase-1-ingest.md` was never
  explicitly marked — Sourav should confirm whether the commit itself counts as approval.
  History below is left unedited; this entry is the correction. Added START-HERE.md,
  `team/plans/INDEX.md`, `research/README.md` and fixed the stale digit-folding note in
  `research/00-api-probe/FINDINGS.md` (L7 correction) in the same pass — see the agent's
  final report for the full file list. · L-refs: none (process/hygiene)

- 2026-08-13 ~16:30 · hackathon (per Sourav) · Aakash's preliminary findings folded in (research/11: two inbox files + SYNTHESIS.md with the PM-spec ruling — receipts foundation kept, action layer adopted on top, pasted-transcript ingestion rejected for API-gravity reasons). Master plan committed at team/plans/master-plan.md (8-stage roadmap, Stage-2 v2 sub-parameters, Stages 5/6 designs, 3 mechanisms deep in v1, real-call calibration directive, privacy rule for the SDR CSV). Pushing all planning artifacts to origin per Sourav; isolated cleanup/coherence agent runs next; BUILD REMAINS GATED (Saritha bake-off + Sourav final gate). · L-refs: L5, L8, L12, L15, L18

- 2026-08-13 ~16:00 · projects-2f (filing for hackathon teardown agent) · anarlog/Hyprnote teardown receipts FILED as CONFIRMED in research/11 (Aakash's lane — he hadn't claimed yet, so filed directly per relay; he inherits and promotes). Headline: citation is architecturally impossible in their enhance path (only {text, speaker} reaches the prompt), yet they built a working evidence-ID citation engine and pointed it at speaker labeling — "provenance as a diarization problem, not a trust problem." Also: demo-plan constraint noted in phase-3-ui.md (never cut click→highlight→audio / uncorroborated bucket / injection line; tier-1 export is the de-risk). · L-refs: supports the §1 wedge; L17

- 2026-08-13 ~15:45 · projects-2f (per Sourav) · REPO HYGIENE PASS for incoming teammates + their LLMs: PROTOCOL.md rewritten v1.0 (adopted; humans in roster; session-genealogy noise removed; pull-before-claim; A1/A2 folded in), internal README.md added (state banner + reading order + repo map; clearly NOT the public launch README), TASKBOARD gains a freeze banner so no fresh LLM builds from stale phase rows (research lanes marked exempt), CLAUDE.md auditor-routing made repo-native (comments+SYNC, not cross-session messaging), phase-1-ingest.md now states its files are invisible to cloners until approved. Historical log entries left untouched — history is history. · L-refs: none (process)

- 2026-08-13 ~15:20 · projects-2f (per Sourav) · REMOTE IS LIVE: https://github.com/souravmohanty-web/opengong-lite (private). From now on a claim counts when PUSHED; pull before claiming. Teammates get invited by GitHub handle, then follow team/ONBOARDING.md. Heads-up from hackathon session logged: master plan will replace horizontal phases with VERTICAL SLICES (Slice 1 = walking skeleton incl. minimal one-interaction viewer); phase-1/phase-3 doc reconciliation HELD until the master plan lands; failing-test-first Iron Law will apply (escaping tests before escaper). · L-refs: none (process)

- 2026-08-13 ~15:00 · projects-2f · Phase-3 blueprint written (planning artifact, freeze respected): team/plans/phase-3-ui.md — one-viewer/three-fuel-lines architecture, 7-state claim→line→audio interaction machine, export writers, tier-2 fragment codec w/ 1,500-char threshold, 5-layer escaping strategy incl. the </script> inline-JSON trap, 10 fixture-driven tests, 5.5h estimate w/ named cut order. 3 open questions flagged for auditor/Sourav (server-vs-file app mode, coverage thresholds, injection demo badge). Grounded in research/02 measured numbers. · L-refs: L4, L6, L7, L10, L11, L17, L19

- 2026-08-13 ~14:30 · projects-2f (per Sourav) · REMOTE COLLAB SCAFFOLDING: root CLAUDE.md (auto-onboards any teammate's Claude session), team/ONBOARDING.md (the pebble loop: claim → find → share → promote → build), research lanes 10 (Saritha, reasoning model — eval criterion is QUOTE FIDELITY per L6, not eloquence) and 11 (Aakash, competitive intel) seeded with the locked constraints they build on. Board gains R-10/R-11 rows; research lanes are exempt from the build freeze (freeze covers code only). Next: push to a private GitHub remote (Sourav choosing account) + invite teammates. · L-refs: L5, L6, L9 (lane 10 constraints); L11, L17, L18 (lane 11 constraints)

- 2026-08-13 ~14:10 · projects-2f · FREEZE ACKED, all code work halted. State at freeze: Phase 1 was already functionally complete — src/{ingest,transcript}.js + test/{transcript.golden,ingest}.test.js in working tree, UNCOMMITTED (left per freeze order); `npm test` 11/11 before the freeze arrived; live e2e verified (call.wav → mono, 2 utterances). ⚠️ CONFLICT FLAGGED: Sourav told projects-2f directly at ~13:45 "Lets do it. whatever is required for next steps" — that green light predates the ~14:05 freeze relay, so the freeze wins on recency, but Sourav is being asked in the projects-2f terminal to confirm which instruction stands. Phase-1 review doc (plan-mode artifact) at team/plans/phase-1-ingest.md for his approve/reject. · L-refs: none (process)

- 2026-08-13 ~14:05 · Sourav (via hackathon) · **BUILD FREEZE — plan mode only, both sessions.** Sourav has not approved the build; hackathon session's "Phase 0 ready" call was premature and is retracted. No code work until explicit green light. Built code stays as-is (no reverts); projects-2f to pause Phase 1 and mark its own board rows. Planning artifacts are allowed. · L-refs: none (process)

- 2026-08-13 ~13:50 · projects-2f · Phase 0 DONE (evidence on board; fresh run this session: `npm test` 3/3, cold `npm start` minted live key + authed `/voices` 144 voices, warm reuse verified). Phase 1 ingest now in progress. Notes: minted key persists to `sandbox.pyai_key` (already gitignored via `*.pyai_key`, verified with `git check-ignore`); 401→re-mint only for `pyai_test_*` keys and only when PYAI_API_KEY is unset; 429 mapped to named exit PYAI_DAILY_CAP. · L-refs: L12, L13, L14, L19
- 2026-08-13 ~13:45 · Sourav (via projects-2f) · Slack connector added; hackathon team talks in the `electron` group — projects-2f will monitor it (read-only MCP) and relay relevant decisions here. · L-refs: none (process)

- 2026-08-13 ~13:30 · projects-2f · CLAIMED Phase 0 (in-progress) + Phase 1 (queued). Onboarding read in order (brief → protocol → board → findings+addendum). Phase-0 file surface: package.json, LICENSE, SECURITY.md, .env.example, capabilities.json, src/{index,pyai,keystore}.js, .github/workflows/ci.yml — no other files will be touched. · L-refs: L12 (capabilities.json), L14 (key-mint + 401 re-mint), L19 (MIT/SECURITY/gitleaks)

- 2026-08-13 ~13:15 · hackathon · PROTOCOL.md ADOPTED w/ amendments A1 (auditor relay for L-changes + pre-merge review) and A2 (commit-per-completed-task). P-1 executed: `git init -b main`, baseline commit `5d24bd6` (evidence: `git log --oneline`, run fresh). · L-refs: none (process)
- 2026-08-13 ~13:15 · hackathon (relaying Sourav) · ROSTER: partner session is fresh `projects-2f`; marketing session removed from project; Phases 0–1 reassigned to projects-2f; Phase 0 marked ready (git done, D1 gates only the public name). · L-refs: L14 (key-mint flow now Phase-0 scope)
- 2026-08-13 · marketing session · Created team/ scaffolding (SYNC.md + TASKBOARD.md seeded verbatim from the brief §3 build-order table). No build started; Phase 0 gated on D1–D5. · L-refs: none (process only)
- 2026-08-13 · hackathon session (relayed) · Guardrail adopted: nobody starts Phase 2+ until the gate-chain owner is named (D5). · touches L-gate chain ownership
- OPEN · Sourav · D1 name/trademark (raise with organizers) · D2 deal-arc → 1:1 calls (2-speaker stereo cap) · D3 real dual-channel recording · D4 Anthropic key + spend cap · D5 owner assignments incl. split confirmation
